; Module loading. autoload keeps res_pjsip + dependencies working across
; package updates. AMI and the HTTP/ARI server are kept OFF via
; manager.conf and http.conf (enabled = no), so nothing privileged is exposed.

[modules]
autoload = yes
