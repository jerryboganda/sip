; Asterisk Manager Interface (AMI) — DISABLED.
; Never expose AMI (5038) publicly. Enable only on localhost if you later add
; a dialer/app, and firewall it to private networks.
[general]
enabled = no
webenabled = no
